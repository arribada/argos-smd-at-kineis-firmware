build/*
build_bl/*
# Stray per-file build artifacts (gcc -MD/-fstack-usage) that can land at the
# repo root when a compile is run outside build/ - never source.
*.d
*.su
Tests/build/*
Tests/unit/build/*
.settings/
.vscode/
Kineis/doc/html/
.externalToolBuilders/
*.log/
*.log
*.bak
*.ioc
.claude/*
Tools/*
# Build-essential tooling referenced by the Makefile (full/dfu targets) must
# ship so CI and fresh clones can build — the rest of Tools/ stays ignored.
!Tools/merge_firmware.py
!Tools/create_dfu.py
!Tools/dfu_tool.py
Bootloader/build/*
Tests/report/
.cache/
compile_commands.json

# Private working docs (audits, reports, deep-dives) — kept LOCAL for us, NOT
# published. The user-facing documentation is the GitHub wiki instead.
/docs/